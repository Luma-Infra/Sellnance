# 🚀 거래소 빌더 함수 리팩토링 (Golden Master 파이프라인 적용)

거대한 `build_binance_row`와 `build_upbit_row` 함수를 단일 책임 원칙에 따라 분리하고, 100% 결과 일치(무결성)를 보장하는 리팩토링을 수행합니다.

> [!CAUTION]
> **중요 공지**: 이 작업은 라이브 데이터 조립의 핵심 엔진을 교체하는 대공사입니다. 따라서 단순 코드 수정을 넘어, 수정 전/후 데이터가 단 1비트의 오차도 없이 일치하는지 수학적으로 검증하는 프로세스(Golden Master Testing)를 반드시 포함하여 진행합니다.

## User Review Required
> [!IMPORTANT]
> 아래의 진행 단계와 분리될 구조를 확인해 주시고 승인(Proceed)해 주시면, 먼저 **데이터 스냅샷 스크립트 작성 및 원본 데이터 추출**부터 안전하게 1단계씩 진행하겠습니다.

## Proposed Changes

### 1. 스냅샷 회귀 검증 스크립트 신설 (안전장치)
#### [NEW] [test_builder_snapshot.py](file:///c:/Users/78831/Sellnance/scratch/test_builder_snapshot.py)
* 리팩토링 전 현재 엔진으로 조립된 전체 코인 800+개의 최종 `dict` 결과를 JSON 파일로 덤프(스냅샷).
* 리팩토링 후 새 엔진으로 돌린 결과와 1:1 대조하여, 수치나 구조에 단 0.0001%의 변경이라도 있으면 에러를 발생시키는 검증기.

---

### 2. 15개 파라미터 통합 컨텍스트 도입 및 공통 로직 추출
#### [NEW] [modules/builder_common.py](file:///c:/Users/78831/Sellnance/modules/builder_common.py)
* **`BuildContext` 데이터 클래스**: `binance_data, upbit_data, krw_usd_rate, mapping, ...` 등 15개의 인자를 하나로 묶어 전달하는 객체.
* **`calc_prices_and_changes()`**: 시가, 종가, 등락률 계산 및 1000배율 보정 로직 (순수 함수)
* **`calc_aggregated_volumes()`**: 업비트/빗썸/바이비트 환율 적용 및 거래대금 합산
* **`calc_kimchi_and_basis()`**: 김치프리미엄, 베이시스 비율 산출
* **`match_counterpart_exchanges()`**: 빗썸/바이비트 등 중복 티커 매칭 로직
* **`resolve_badges_and_tags()`**: 상장 거래소 목록 및 UI 뱃지 스트링(HTML 렌더링 무관 순수 텍스트 배열) 판별기

---

### 3. 괴물 함수(God Functions) 파이프라인화
#### [MODIFY] [modules/builder_binance.py](file:///c:/Users/78831/Sellnance/modules/builder_binance.py)
#### [MODIFY] [modules/builder_upbit.py](file:///c:/Users/78831/Sellnance/modules/builder_upbit.py)
* 684줄/434줄의 코드를 약 50줄 이내의 오케스트레이터(Orchestrator) 함수로 대폭 축소.
* `BuildContext` 인스턴스를 받아 `builder_common.py`의 공통 함수들을 순차적으로 호출한 뒤 기존과 100% 동일한 키(Key)를 가진 `dict`를 반환하도록 인터페이스 유지.

---

### 4. 호출부 (메인 빌더) 인자 수정
#### [MODIFY] [modules/builder.py](file:///c:/Users/78831/Sellnance/modules/builder.py)
* `build_binance_row`와 `build_upbit_row`를 호출할 때 낱개 변수 15개를 나열하지 않고 `BuildContext` 객체를 생성하여 넘기도록 호출부 깔끔하게 수정.

## Verification Plan

### Automated Tests
1. 리팩토링 전, `test_builder_snapshot.py --save` 실행하여 원본 스냅샷(JSON) 확보
2. 리팩토링 진행 (`builder_common.py` 작성 및 함수 쪼개기)
3. 리팩토링 후, `test_builder_snapshot.py --verify` 실행하여 스냅샷 100% 일치 확인
4. 단 한 키라도 `deepdiff` 상 오차가 발생하면 즉각 롤백 후 원인 파악 (오차 0% 달성 시까지 커밋 보류)
